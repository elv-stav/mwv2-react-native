import { colors } from './colors';
import { scale } from './scale';
import { sizes } from './sizes';
import { spacings } from './spacings';
import { typography } from './typography';

export const theme = { spacings, sizes, colors, typography, scale };
